<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real theme; this only needs to give the window a
         calm background and no action bar while the activity starts. -->
    <style name="Theme.SbtSurvey" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/sbt_surface</item>
    </style>
</resources>
